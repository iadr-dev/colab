---
description: Enter persistent compressed communication mode
---

# /caveman

Activate `caveman` mode for this session.

The agent should:
- load `skills/caveman/SKILL.md`
- respond with compressed wording while preserving technical accuracy
- stay in compressed mode until the user says `stop caveman` or `normal mode`

To exit:

```text
stop caveman
```
